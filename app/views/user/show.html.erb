


<script charset="utf-8">
  var currentBeer = <%=@user.beer%>
  checkStatus();
</script>

<div class = "container-fluid">
  <section class= "row">
    <div class= "col-md-6 clearfix" id="header">
        <!-- make a brew logo, insert next to Brew Log -->
        <h1 id="textLogo" class="text-left">Brew Log<img class="smLogo" src="/assets/beer_glasses.jpg"/></h1>
    </div>
    <!-- the navbar for user -->
    <div class="col-md-6 clearfix text-right" id="userNav">
      <h3 id="welcomeUser"> Hello, <%= @user.username %> |</h3>
      <form id="formLogout" action='/session' method='POST'>
        <input type="hidden" name="authenticity_token" value="<%=form_authenticity_token()%>">
        <input type="hidden" name="_method" value="DELETE">
        <input id="logoutUser" type="submit" name="name" value="Logout">
      </form>
    </div>
  </section>
  <section class="row">
    <div class="col-md-12">
      <!-- <h1><%= @user.username %>, YOU'VE MADE IT!</h1> -->
    </div>
  </section>
  <section class="row text-center">
    <div class="col-md-4">
      <div id="cellar">
        <h4>CELLAR</h4>
      </div>
    </div>
    <div class="col-md-4">
      <div id="newBrew">
        <h4>BREW</h4>
      </div>
    </div>
    <div class="col-md-4">
      <% if @user.beer %>
        <div id="progress"> </div>
          <%= @user.beer.beer_name %>
        <!-- here you should render the progress bar, or call the function that does -->
      <% else %>
        <div id="noAction">
            <h4>GET BREWIN!</h4>
        </div>
      <%end%>
    </div>
  </section>
</div>
